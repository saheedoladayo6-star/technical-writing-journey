# Markdown Code Blocks
## Introduction
Code blocks are used to display commands or code clearly.
## Example
```bash
git status
git add .
git commit -m "Initial commit"
git push
```
## Inline Code
Use backticks to highlight commands like `git status` or `code .`.
## Conclusion
Code blocks make documentation easier to read and copy.

